<UserControl x:Class="Pazaak.UserControls.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pazaak.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="50" BorderBrush="Purple" BorderThickness="1" MinWidth="{Binding Width, ElementName=labelDisplay}" MinHeight="{Binding Height, ElementName=labelDisplay}">
    <Grid MouseEnter="Grid_MouseEnter" MouseLeave="Grid_MouseLeave" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <Label x:Name="labelDisplay" Padding="0"
               Content="{Binding Display}" 
               FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
               HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
